# SOURCE ALIGNMENT NOTES

The attached master blueprint defines the core product as a narrow AI layer between fragmented field reporting and detailed L5/L6 schedules. It explicitly says the real innovation is event-to-schedule matching + confidence + human validation, not OCR, chatbot, or dashboard.

The blueprint's MVP scope is:
- Excel schedule import
- DPR PDF/text upload
- LLM structured extraction
- L5/L6 matching
- confidence score + tiered routing
- planner approval UI
- progress dashboard
- audit trail
- synthetic dataset

The blueprint explicitly defers live Primavera/MS Project integration, enterprise authentication, mobile app, predictive analytics, and production multilingual support.

Use these source-aligned facts as constraints. Do not add unrelated features merely because they are technically impressive.
